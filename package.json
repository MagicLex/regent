{
  "name": "regent-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "cd client && npm run dev",
    "start": "cd client && npm run preview-prod"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "ai",
    "chat",
    "sovereign",
    "hopsworks",
    "regent"
  ]
}